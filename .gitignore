# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual envs
.venv/
venv/
env/

# uv
.uv/

# Tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# Local overrides — never commit
.env
*.local.*
.claude/settings.local.json
CLAUDE.local.md

# Secrets — never commit (local source-of-truth for keys/toggles)
API-KEYS-AND-SETTINGS.md
*KEYS*.md
*.key
# Secret-bearing config/data files. Narrowed from the old bare `*secret*`, which
# silently ignored ANY path containing "secret" — including source/tests (it
# swallowed tests/chains/test_chain_redacts_secrets.py during step10 S.1).
# Scoped to data/config extensions so it never eats .py/.md source again.
*.secret
*.secrets
secrets.json
secrets.yaml
secrets.yml
secrets.toml
secrets.env
client_secret*.json
.env.secret

# Git worktrees (embedded repos under .claude/)
.claude/worktrees/

# Generated artifacts
docs/graphify/

# Local planning docs — keep out of new commits. NOTE: step5–9.md are already
# tracked and stay on origin; gitignore does not untrack them. This only stops
# brand-new step*.md (and the local step10.md working copy) from being added.
step*.md

# Local go-to-market / strategy / brain-dump docs — NEVER commit (repo is public;
# these hold pricing, targeting, post drafts, and competitive analysis).
launch/
launch.md
marketing.md
new_websites.md
new.md
remaining*.md
mcp-builder/

# Internal build-process / audit docs — untracked 2026-06-06 (were committed before
# this rule). Kept local; no secrets, just behind-the-scenes notes not meant for the
# public repo. Already-removed history copies are harmless (no credentials).
AUDIT.md
BACKLOG.md
changes.md
plan.md
docs/hot.md
LEARNING-GUIDE.md

# Completed/archived planning docs — moved out of the root once their work shipped.
# Local-only; never commit. Anything inside is finished history, not live work.
done/

# Monetization — strategy docs + the LIVE Pro key ledger. NEVER commit (repo is
# public; sponsor-ledger.md holds the working SPORTIQ_PRO_KEY — committing it
# would unlock Pro for everyone for free).
sponsor-ledger.md
monetization-paths.md
monetization-progress.md
reframe.md
v1.md
v2.md
v3.md
v2a-testing-checklist.md
free-rollback-plan.md

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/

# diskcache local store
.cache/

# Offline calibration datasets — raw data NEVER committed (only derived seeds in
# src/.../data/*.json are). See new_dataset.md / scripts/calibrate_*.py.
datasets/

# Local analytics dashboard (scripts/dashboard.py) — generated output + cache.
# Read-only personal monitoring; never committed.
.dashboard_cache/
dashboard.html

# Local dev/process artifacts — repo is public; these expose calibration
# methodology, the ops log, and in-progress planning. Kept local only.
# docs/log.md, dev.md, scripts/dashboard_template.html were committed before and
# are untracked via `git rm --cached` (working copies stay on disk; past versions
# remain in git history).
new_dataset.md
website.md
scripts/backtest_*.py
scripts/calibrate_*.py
docs/log.md
dev.md
scripts/dashboard_template.html

# Codex review artifacts — local-only working notes from the codex audit pass.
# Never commit (behind-the-scenes review chatter, not live repo docs).
codex_changes*.md
codex_suggestions.md

# Personal interview-prep notes — local only, not project docs.
interview_explanation.md

# Oversized demo-media variants — the repo already tracks ~11 MiB of demo media;
# these regenerated GIF/MOV variants are 14–29 MiB each. Kept local only.
docs/assets/sportiq_new*.gif
docs/assets/sportiq_new*.mov
